apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: {{ .Values.stages.qa | quote }}
  namespace: {{ .Values.projectName | quote }}
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: {{ .Values.projectName | quote }}
      sources:
        stages:
          - {{ .Values.stages.dev | quote }}
